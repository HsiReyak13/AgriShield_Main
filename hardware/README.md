# AgriShield Hardware

Hardware work for the AgriShield prototype belongs here.

## Folders

- `firmware/` - ESP32 firmware projects and source code.
- `docs/` - wiring notes, sensor references, setup notes, and device documentation.

Keep real device secrets out of git. Use example config files for values such as Wi-Fi credentials, Firebase URLs, and device codes.
