# Firmware

Place ESP32 firmware projects here.

Recommended future structure:

```text
hardware/firmware/
  agri_sensor/
    src/
    include/
    test/
    README.md
```

Use `config.example.h` or an equivalent example file for required settings, and keep real `config.h` files ignored.
